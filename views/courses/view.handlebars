<div class="jumbotron">
  <h1 class="display-4">{{course.title}}</h1>
  <p class="lead">{{course.description}}</p>
  <p>For Grade {{course.grade}}</p>
  <hr class="my-4">
  <a class="btn btn-primary" href="#" role="button">Add video</a>
</div>